Your statement is **partially correct, but not fully correct**.

### 1. "`LlmAgent` is wrong" → ❌ Not necessarily

`LlmAgent` is correct **if the agent uses Gemini reasoning/instructions**. ADK root orchestration agents commonly use `LlmAgent` for planning, routing, validation, and coordination. ([Google][1])

But if Agent 1 is designed as:

```text
Pure workflow controller
No reasoning
No planning
No model decisions
Only route functions
```

Then an LLM agent is unnecessary.

For your Nivedan AI orchestrator ("brain", retries, coordination, planning), `LlmAgent` is reasonable. 

---

### 2. "`new InMemoryMemoryService()` locally is wrong" → ✅ Mostly correct

This part is important.

ADK recommends **shared/singleton session and memory services** so state persists consistently across runners. Creating isolated instances can fragment state. ([Google][2])

Better:

```ts
sessionService (singleton)

memoryService (singleton)
```

Not:

```ts
const memoryService = new InMemoryMemoryService()
```

inside every agent file.

---

### 3. "Agent 1 needs zero LLM calls" → ❌ Depends on architecture

For your Nivedan AI:

Current design:

```text
Orchestrator
↓
Planning
Retry decisions
Execution ordering
State validation
Context compression
Failure handling
```

Those benefit from Gemini reasoning.

So:

```text
LLM calls = YES
```

If you redesign Agent 1 into:

```text
Simple router only
```

Then:

```text
LLM calls = NO
```

---

### 4. "Must use singleton sessionService" → ✅ Correct

ADK memory/session architecture strongly favors shared services. ([Google][2])

---

### Final verdict for Nivedan AI

```text
LlmAgent → KEEP ✅

Singleton sessionService → YES ✅

Singleton memoryService → YES ✅

No LLM calls → NO ❌
```

For your architecture, your original direction is stronger because your Orchestrator is intended to be **the intelligent workflow brain**, not only a task router. 

[1]: https://google.github.io/adk-docs/api-reference/typescript/?utm_source=chatgpt.com "ADK for TypeScript: API Reference"
[2]: https://google.github.io/adk-docs/sessions/memory/?utm_source=chatgpt.com "Memory - Agent Development Kit (ADK)"
